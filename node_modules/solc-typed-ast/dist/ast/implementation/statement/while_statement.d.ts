import { ASTNode } from "../../ast_node";
import { Expression } from "../expression/expression";
import { StructuredDocumentation } from "../meta";
import { Statement } from "./statement";
export declare class WhileStatement extends Statement {
    /**
     * Continuation condition, e.g. `x < 10` in `while (x < 10) { ... }`
     */
    vCondition: Expression;
    /**
     * Statement that gets executed if the condition is true
     */
    vBody: Statement;
    constructor(id: number, src: string, condition: Expression, body: Statement, documentation?: string | StructuredDocumentation, raw?: any);
    get children(): readonly ASTNode[];
}
//# sourceMappingURL=while_statement.d.ts.map