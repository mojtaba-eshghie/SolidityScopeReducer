import { ASTNode } from "../../ast_node";
import { Expression } from "../expression/expression";
import { StructuredDocumentation } from "../meta";
import { Statement } from "./statement";
/**
 * An expression, that is specified on a statement level.
 */
export declare class ExpressionStatement extends Statement {
    /**
     * A contained expression, e.g. `foo(1);` or `x = 1 + 1;`
     */
    vExpression: Expression;
    constructor(id: number, src: string, expression: Expression, documentation?: string | StructuredDocumentation, raw?: any);
    get children(): readonly ASTNode[];
}
//# sourceMappingURL=expression_statement.d.ts.map