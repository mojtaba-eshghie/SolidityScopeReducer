"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ModernFunctionCallOptionsProcessor = void 0;
const expression_processor_1 = require("./expression_processor");
class ModernFunctionCallOptionsProcessor extends expression_processor_1.ModernExpressionProcessor {
    process(reader, config, raw) {
        const [id, src, typeString] = super.process(reader, config, raw);
        const names = raw.names;
        const expression = reader.convert(raw.expression, config);
        const values = reader.convertArray(raw.options, config);
        const options = new Map();
        for (let n = 0; n < names.length; n++) {
            options.set(names[n], values[n]);
        }
        return [id, src, typeString, expression, options, raw];
    }
}
exports.ModernFunctionCallOptionsProcessor = ModernFunctionCallOptionsProcessor;
//# sourceMappingURL=function_call_options_processor.js.map