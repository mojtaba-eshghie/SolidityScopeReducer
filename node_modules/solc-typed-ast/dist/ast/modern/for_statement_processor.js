"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ModernForStatementProcessor = void 0;
const node_processor_1 = require("./node_processor");
class ModernForStatementProcessor extends node_processor_1.ModernNodeProcessor {
    process(reader, config, raw) {
        const [id, src] = super.process(reader, config, raw);
        const documentation = raw.documentation;
        const initializationExpression = raw.initializationExpression
            ? reader.convert(raw.initializationExpression, config)
            : undefined;
        const condition = raw.condition
            ? reader.convert(raw.condition, config)
            : undefined;
        const loopExpression = raw.loopExpression
            ? reader.convert(raw.loopExpression, config)
            : undefined;
        const body = reader.convert(raw.body, config);
        return [
            id,
            src,
            body,
            initializationExpression,
            condition,
            loopExpression,
            documentation,
            raw
        ];
    }
}
exports.ModernForStatementProcessor = ModernForStatementProcessor;
//# sourceMappingURL=for_statement_processor.js.map