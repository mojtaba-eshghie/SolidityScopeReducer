"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ModernInlineAssemblyProcessor = void 0;
const node_processor_1 = require("./node_processor");
class ModernInlineAssemblyProcessor extends node_processor_1.ModernNodeProcessor {
    process(reader, config, raw) {
        const [id, src] = super.process(reader, config, raw);
        const externalReferences = raw.externalReferences;
        const documentation = raw.documentation;
        const operations = raw.operations;
        const yul = raw.AST;
        const flags = raw.flags;
        const evmVersion = raw.evmVersion;
        return [
            id,
            src,
            externalReferences,
            operations,
            yul,
            flags,
            evmVersion,
            documentation,
            raw
        ];
    }
}
exports.ModernInlineAssemblyProcessor = ModernInlineAssemblyProcessor;
//# sourceMappingURL=inline_assembly_processor.js.map