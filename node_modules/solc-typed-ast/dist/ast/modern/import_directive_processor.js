"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ModernImportDirectiveProcessor = void 0;
const node_processor_1 = require("./node_processor");
class ModernImportDirectiveProcessor extends node_processor_1.ModernNodeProcessor {
    process(reader, config, raw) {
        const [id, src] = super.process(reader, config, raw);
        const file = raw.file;
        const absolutePath = raw.absolutePath;
        const unitAlias = raw.unitAlias;
        const symbolAliases = raw.symbolAliases;
        const scope = raw.scope;
        const sourceUnit = raw.sourceUnit;
        for (const alias of symbolAliases) {
            if (typeof alias.foreign !== "number") {
                alias.foreign = reader.convert(alias.foreign, config);
            }
        }
        return [id, src, file, absolutePath, unitAlias, symbolAliases, scope, sourceUnit, raw];
    }
}
exports.ModernImportDirectiveProcessor = ModernImportDirectiveProcessor;
//# sourceMappingURL=import_directive_processor.js.map