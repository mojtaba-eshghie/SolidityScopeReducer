"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BuiltinFunctionType = void 0;
const function_like_type_1 = require("./function_like_type");
/**
 * The type of a builtin function
 */
class BuiltinFunctionType extends function_like_type_1.FunctionLikeType {
    constructor(name, parameters, returns, src) {
        super(name, parameters, src);
        this.returns = returns;
    }
    pp() {
        const mapper = (node) => node.pp();
        const argStr = this.parameters.map(mapper).join(",");
        let retStr = this.returns.map(mapper).join(",");
        retStr = retStr !== "" ? ` returns (${retStr})` : retStr;
        return `builtin_function ${this.name !== undefined ? this.name : ""}(${argStr})${retStr}`;
    }
}
exports.BuiltinFunctionType = BuiltinFunctionType;
//# sourceMappingURL=builtin_function.js.map